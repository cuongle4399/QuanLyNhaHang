<Window x:Class="restaurantManager.Staff"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:restaurantManager"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:restaurantManager.View.Fuction.Staff"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/Image/logo/logo.ico"
        Title="Nhân Viên"
        Height="600"
        MinHeight="600"
        MinWidth="1000"
        Width="1000">

    <Grid>
        <materialDesign:Card>
            <TabControl 
                        SnapsToDevicePixels="True"
                        Style="{StaticResource MaterialDesignNavigationRailTabControl}"
                        TabStripPlacement="Left"
                        materialDesign:NavigationRailAssist.ShowSelectionBackground="True">

                <!-- Tab Gọi món -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel HorizontalAlignment="Center">
                            <materialDesign:PackIcon Width="24" Height="24" Kind="Food" HorizontalAlignment="Center"/>
                            <TextBlock Text="Gọi món" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </TabItem.Header>
                    <StackPanel Margin="16">
                        <views:orderFood/>
                    </StackPanel>
                </TabItem>

                <!-- Tab Tính tiền -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel HorizontalAlignment="Center">
                            <materialDesign:PackIcon Width="24" Height="24" Kind="CreditCardSettingsOutline" HorizontalAlignment="Center"/>
                            <TextBlock Text="Tính tiền" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </TabItem.Header>
                    <StackPanel Margin="16">
                        <views:confirmPayFood/>
                    </StackPanel>
                </TabItem>

                <!-- Tab Đăng xuất -->
                <TabItem x:Name="LogoutTab" PreviewMouseDown="LogoutTab_PreviewMouseDown">
                    <TabItem.Header>
                        <StackPanel HorizontalAlignment="Center">
                            <materialDesign:PackIcon Width="24" Height="24" Kind="Logout" HorizontalAlignment="Center"/>
                            <TextBlock Text="Đăng xuất" HorizontalAlignment="Center" TextWrapping="Wrap" Margin="0,4,0,0"/>
                        </StackPanel>
                    </TabItem.Header>
                </TabItem>

            </TabControl>
        </materialDesign:Card>
    </Grid>
</Window>
