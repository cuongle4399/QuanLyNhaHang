<Window x:Class="restaurantManager.Staff"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:restaurantManager"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:restaurantManager.View.Fuction.Staff"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Icon="/Resources/Image/logo/logo.ico"
        Title="Nhân Viên"
        Height="600"
        MinHeight="600"
        MinWidth="1000"
        Width="1000">

    <Grid Background="#2E2E2E">
        <materialDesign:Card Background="#2E2E2E" Margin="0" Padding="0">
            <TabControl  Background="#2E2E2E"
                        SnapsToDevicePixels="True"
                        Style="{StaticResource MaterialDesignNavigationRailTabControl}"
                        TabStripPlacement="Left"
                        materialDesign:NavigationRailAssist.ShowSelectionBackground="True"
                        Foreground="White">
                <!-- TabItem chữ mặc định trắng -->

                <!-- Tab Gọi món -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel HorizontalAlignment="Center">
                            <materialDesign:PackIcon Width="24" Height="24" Kind="Food" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Gọi món" HorizontalAlignment="Center" Foreground="White"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="16">
                        <views:orderFood/>
                    </Grid>
                </TabItem>

                <!-- Tab Tính tiền -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel HorizontalAlignment="Center">
                            <materialDesign:PackIcon Width="24" Height="24" Kind="CreditCardSettingsOutline" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Tính tiền" HorizontalAlignment="Center" Foreground="White"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="16">
                        <views:confirmPayFood/>
                    </Grid>
                </TabItem>

                <!-- Tab đổi mật khẩu -->
                <TabItem>
                    <TabItem.Header>
                        <StackPanel HorizontalAlignment="Center">
                            <materialDesign:PackIcon Width="24" Height="24" Kind="Food" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Đổi mật khẩu" TextWrapping="Wrap" TextAlignment="Center"  HorizontalAlignment="Center" Foreground="White"/>
                        </StackPanel>
                    </TabItem.Header>
                    <Grid Margin="16">
                        <views:changePassword/>
                    </Grid>
                </TabItem>

                <!-- Tab Đăng xuất -->
                <TabItem x:Name="LogoutTab" PreviewMouseDown="LogoutTab_PreviewMouseDown">
                    <TabItem.Header>
                        <StackPanel HorizontalAlignment="Center">
                            <materialDesign:PackIcon Width="24" Height="24" Kind="Logout" Foreground="White" HorizontalAlignment="Center"/>
                            <TextBlock Text="Đăng xuất" HorizontalAlignment="Center" TextWrapping="Wrap" Margin="0,4,0,0" Foreground="White"/>
                        </StackPanel>
                    </TabItem.Header>
                </TabItem>

            </TabControl>
        </materialDesign:Card>
    </Grid>
</Window>
